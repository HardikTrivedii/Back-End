import { Request, Response, NextFunction } from "express";
export class Middleware {
  // public checkForUniqueEmail = async (
  //   req: Request,
  //   res: Response,
  //   next: NextFunction
  // ) => {
  //   const { email } = req.body;
  //   const result = await this.authUtils.checkUserExists(email);
  //   if (result) {
  //     return result;
  //   }
  //   next();
  // };
}
